{
  "name": "visionos-ar",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html style.css app.js dist/",
    "sync": "npx cap sync capacitor-android",
    "open": "npx cap open capacitor-android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0",
    "typescript": "^5.0.0"
  }
}
